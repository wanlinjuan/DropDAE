% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_triplet_indices.R
\name{generate_triplet_indices}
\alias{generate_triplet_indices}
\title{Generate Triplet Indices for Contrastive Learning}
\usage{
generate_triplet_indices(
  consensus_matrix,
  batch_indices,
  pos_threshold = 0.8,
  neg_threshold = 0.2
)
}
\arguments{
\item{consensus_matrix}{A numeric matrix representing cell-to-cell similarities (values between 0 and 1).}

\item{batch_indices}{A vector of integers indicating the indices of the current batch.}

\item{pos_threshold}{A threshold above which cells are considered positive pairs. Default is 0.8.}

\item{neg_threshold}{A threshold below which cells are considered negative pairs. Default is 0.2.}
}
\value{
A list containing:
\describe{
  \item{pos_indices}{Vector of positive indices corresponding to each anchor cell.}
  \item{neg_indices}{Vector of negative indices corresponding to each anchor cell.}
}
}
\description{
This function generates triplet indices for contrastive loss training,
based on a consensus matrix of cell-cell similarities.
}
\examples{
set.seed(1)
consensus <- matrix(runif(100), nrow = 10)
batch <- 1:10
triplets <- generate_triplet_indices(consensus, batch)
str(triplets)
}

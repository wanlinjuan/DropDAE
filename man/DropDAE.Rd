% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DropDAE.R
\name{DropDAE}
\alias{DropDAE}
\title{DropDAE: Denoising Autoencoder with Triplet Loss for scRNA-seq Data}
\usage{
DropDAE(
  training_data,
  test_data,
  normalization = "sct",
  num_epochs = 300,
  batch_size = 1000,
  lr = 0.01,
  hidden_size = c(64, 32, 64),
  lambda = 10,
  k = 2,
  patience = 5,
  margin = 10,
  num_initializations = 1,
  apply_early_stopping = TRUE,
  lr_scheduler = TRUE,
  select_genes = NULL,
  activation = "relu",
  weight_decay = TRUE,
  cc_runs = 50,
  BN = TRUE,
  gradient_clip = TRUE,
  clip_value = 5,
  seed = seed
)
}
\arguments{
\item{training_data}{Matrix of raw training counts.}

\item{test_data}{Matrix of raw testing counts.}

\item{normalization}{Normalization method: "sct" or "log". Default is "sct".}

\item{num_epochs}{Number of training epochs. Default is 300.}

\item{batch_size}{Mini-batch size for training. Default is 1000.}

\item{lr}{Initial learning rate. Default is 0.01.}

\item{hidden_size}{Vector specifying hidden layer sizes. Default is c(64, 32, 64).}

\item{lambda}{Weighting for the triplet loss component. Default is 10.}

\item{k}{Number of clusters used for consensus clustering. Default is 2.}

\item{patience}{Number of epochs with no improvement before early stopping. Default is 5.}

\item{margin}{Margin for triplet loss. Default is 10.}

\item{num_initializations}{Number of random initializations to try. Default is 1.}

\item{apply_early_stopping}{Whether to apply early stopping. Default is TRUE.}

\item{lr_scheduler}{Whether to apply learning rate decay. Default is TRUE.}

\item{select_genes}{Optional vector of genes to subset before training.}

\item{activation}{Activation function: "tanh" or "sigmoid" or "relu". Default is "tanh".}

\item{weight_decay}{Whether to apply L2 regularization. Default is TRUE.}

\item{cc_runs}{Number of consensus clustering runs. Default is 50.}

\item{BN}{Whether to apply batch normalization. Default is TRUE.}

\item{gradient_clip}{Whether to apply gradient clipping. Default is TRUE.}

\item{clip_value}{Maximum allowed gradient norm if clipping is used. Default is 5.0.}
}
\value{
A list containing:
\describe{
  \item{best_x_hat}{Matrix of denoised and imputed test data.}
  \item{loss_data_all_df}{Data frame recording loss curves across training epochs.}
}
}
\description{
This function trains a denoising autoencoder using corrupted scRNA-seq data,
with optional triplet loss based on consensus clustering to improve clustering and imputation.
}
\examples{
\dontrun{
# Example using simulated scRNA-seq data
}
}
